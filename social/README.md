# Social preview image

`og.webp` is the Open Graph / Twitter card image (1200×630) referenced by
`index.html` and `src/data/site.ts` (`social.ogImage`).

Replace `og.webp` with your own 1200×630 image to change the link preview.
It is a static local asset — never uploaded to any third-party service.
