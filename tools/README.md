# Tool icons

Place optional WebP icons here and reference them in `src/data/tools.ts`,
for example `icon: '/tools/my-tool.webp'`.

Tools share the project card layout, including the image fallback: missing or
failed icons display the first letter of the tool name.
